//state struct which should be filled on "2d ticker" initialization
class entity_state
{
    name state_name;//name of the state(used for state searching)
    textureid state_picture;//picture for the state
    double state_time;//time (in 1/35 seconds) which screen entity should show some picture, before changin it
}
